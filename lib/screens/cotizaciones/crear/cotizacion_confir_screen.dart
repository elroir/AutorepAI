import 'package:flutter/material.dart';

class CotizacionConfirScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('header')
      ),
    );
  }
}