import 'package:flutter/material.dart';

class CotizacionBodyScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('body')
      ),
    );
  }
}